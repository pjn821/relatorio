mytyping
keybr
monkeytyping


- quais sao os tipos de operadores que existem no c++/programacao
    aritméticos (\(+\), \(-\), \(*\), \(/\)),
    relacionais (\(<\), \(>\), \(==\), \(!=\)),
    lógicos (\(&&\), \(||\), \(!\)),  
    atribuição (\(=\)), 
    bit a bit (como & e |),  
    deslocamento (\(<<\), \(>>\)),  
    unários (como ++ e --).

- explicar como definir e utilizar arrays
    definir
        tipo_de_dados nome_do_array[tamanho];
        |string|notas | [5]
    declarar
        int meuArray[5] = {10, 20, 30, 40, 50}; // posso inicializar com valores ja definidos 
        int meuArray[5] = {10, 20}; // Equivalente a {10, 20, 0, 0, 0}
        int meuArray[] = {10, 20, 30}; // O compilador define o tamanho como 3
        int numeros[5];
        numeros[0] = 10;
        numeros[1] = 20;
    utilizar

        //#include <iostream>

        //int main() {
            //int numeros[] = {10, 20, 30, 40, 50};

            // Acessar o primeiro elemento (índice 0)
            //std::cout << "Primeiro elemento: " << numeros[0] << std::endl; // Output: 10

            // Acessar o terceiro elemento (índice 2)
            //std::cout << "Terceiro elemento: " << numeros[2] << std::endl; // Output: 30

            // Modificar um elemento (índice 0)
            //numeros[0] = 5;
            //std::cout << "Novo primeiro elemento: " << numeros[0] << std::endl; // Output: 5

            //return 0;
    outra forma 
        // Array 2x3 (2 linhas, 3 colunas)
        int matriz[2][3] = {
            {1, 2, 3}, // Primeira linha (índice 0)
            {4, 5, 6}  // Segunda linha (índice 1)
        };

        // Acessar um elemento:
        int elemento = matriz[0][1]; // Acessa o elemento na linha 0, coluna 1 (valor 2)
    
challenges [[251103-funçao]]

    oque quero que apareca
    terminal= 2 + 2 = 4
     string display1 = a +" + "+ b +" = "+ aqui nao sei 
    