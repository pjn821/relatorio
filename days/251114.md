mytyping
keybr
monkeytyping 


## mat
    matrizes
    fez exercicios sobre 

## sql
    Criação e gestão de bases de dados e tabelas: Uso de comandos como CREATE, DROP, USE com condições como IF NOT EXISTS e IF EXISTS.

    Carregamento de dados a partir de ficheiros CSV: Utilização do comando LOAD DATA INFILE, com opções de delimitadores, carateres de citação e ignorar cabeçalhos; uso de variáveis temporárias para modificar dados durante a importação.

    Transformação dos dados na carga: Emprego de condições (IF, CASE) para categorizar ou modificar os dados ao inserir na tabela.
    
    instalação de mariadb
    criaçao de db e tabelas para importar os doc.csv

## nosql

    FILTROS
→ Comparação: $gt, $lt, $gte, $lte
→ Listas: $in, $nin
→ Lógica: $and, $or
→ Datas: ISODate()

    PROJEÇÃO
→ Campos específicos
→ Campos aninhados
→ Remover _id

    ORDENAÇÃO
→ sort({ campo: 1/-1 })
→ vários campos
→ limitar resultados


porque 


## isalnum

[[251114--isalnum]]

nao percebeu matrizes
o que e` que fizeram no sql?


### nosql tests
// db.createCollection("nome")
db.createCollection("filmes")
//{ } curly brackets
//[ ] square brackets
// db.filmes.insertOne({titulo: "maravilha",nota: 9})
db.filmes.insertMany([
  {titulo: "avatar",nota: 8, assistido: true},
  {titulo: "scarface",nota: 6, assistido: true},
  {titulo: "maravilha",nota: 9, assistido: true},
  {titulo: "paprika",nota: 5, assistido: true},
  {titulo: "wicked",nota: 4, assistido: false},
  {titulo: "wicked 2", assistido: false},
  ])


// select movie
// db.filmes.find()

// movie with nota > 6
// $gt - greater than
// $lt - less than
// $gte - greater than or equal
// $lte - less than or equal
// db.filmes.find({ nota: { $gte: 6 } })
// db.filmes.find({ assistido: true })

// filter and return
// db.filmes.find(
//   { assistido: true },
//   { nota: 1, titulo: 1 }
// )

// hides _id
// db.filmes.find(
//   { assistido: true },
//   { nota: 1, titulo: 1, _id: 0  }
// )

// hides titulo and id
// db.filmes.find(
//   { assistido: true },
//   { nota: 1, titulo: 1, _id: 0  }
// )


// descending order
// db.filmes.find(
//   {},
//   { nota: 1, titulo: 1, _id: 0  }
// ).sort({nota: - 1 })

// ascending order
db.filmes.find(
  {},
  { nota: 1, titulo: 1, _id: 0  }
).sort({nota: -1 })


### IP
#include <iostream>
using namespace std;

int main() 
{
  
  // main()
  // string.length()
  // sizeof() -> return size in bytes
  
  string name = "alice";
  string cores[] = {"red", "blue", "green"};
  int age = 10;
  int idades[] = {93, 38, 82, 83, 980};
  // 2^32 - 4'294'967'296
                  // 4,  4,  4
  bool watched = false;
  
  // cout << sizeof(name) << endl;
  // 32 ??
  // cout << sizeof(age) << endl;
  // int 00000000-00000000-00000000-00000000
  // 11111111-11111111-11111111-11111111-
  // 2^32
  // cout << sizeof(watched) << endl;
  
  // cout << sizeof(age) << endl;
  // cout << sizeof(idades) << endl;
  
  // int size_idades = sizeof(idades)/sizeof(idades[0]);
  // cout << size_idades << endl;
  
  // string.length()
  // sizeof()
  
  int cores_size = sizeof(cores)/sizeof(cores[0]);
  
  for (int i = 0; i < cores_size; i++){
    cout << cores[i] << endl;
  }
}