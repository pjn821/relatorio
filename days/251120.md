mytyping
keybr
monkeytyping 


## as lab5

### comando
```powershell
 # Define a localização do ficheiro CSV
$caminhoCSV = "C:\Scripts\novos_utilizadores.csv"

# Importa os dados do CSV
# Usamos UTF8 para máxima compatibilidade
$dadosUtilizadores = Import-Csv -Path $caminhoCSV -Encoding UTF8

# Itera sobre cada utilizador
foreach ($utilizador in $dadosUtilizadores) {
    # 💥 PONTO CHAVE: Converte a string de password do CSV para um objeto SecureString
    # O comando -Force é necessário para converter a string de texto simples.
    $passwordSegura = ConvertTo-SecureString -String $utilizador.Password -AsPlainText -Force

    Write-Host "A criar o utilizador: $($utilizador.Name)..."

    # Cria o novo utilizador no Active Directory
    New-ADUser `
        -Name $utilizador.Name `
        -GivenName $utilizador.GivenName `
        -Surname $utilizador.Surname `
        -SamAccountName $utilizador.SamAccountName `
        -Path $utilizador.Path `
        -AccountPassword $passwordSegura ` # <-- Usa a SecureString convertida
        -Enabled $true `
        -ChangePasswordAtLogon $true # Força a alteração da password para garantir a segurança

    Write-Host "Utilizador $($utilizador.Name) criado com sucesso." -ForegroundColor Green
}
```
## ip homework

[[251120-iphw]]
// 1- Exercícios para utilização do if()
//         Peça 2 valores ao utilizador (podem ser positivos ou negativos)
//         Se a soma for positiva, escreva no ecrã "Soma positiva"
//         Se a soma for negativa, escreva no ecrã "Soma negativa"
//         Se a soma for zero, escreva no ecrã "Soma nula"

// 2- Peça ao utilizador o valor do raio de um círculo (com decimais)
//         Diga qual a área e qual o perímetro.
//         - Pi= 3,1416
//         - A=Pi*r^2;
//         - P=2*Pi*r;

## revisao mat

identide(I) é a base ou posicao inicial.
A*I=Ia
ex: quardar posicao do jogador num jogo
    na edicao de fotos, posso guardar posicao caso algo corra mal. 
    "ctrl+z"
## sql
```sql
SELECT Localizacao FROM equipas
WHERE lisboa;
```

dados on localizacao e` lisboa
selecionar dados

```sql
-- select tudo
-- select tudo donde?
-- select tudo aonde o qu^e 
-- aonde ha conexao com lisboa
-- procura de lisboa na coluna localizacao
-- lisboa in column localizacao

select * equipas where Localizacao in ('Lisboa');🟥

🟩
SELECT * From equipas
-- where Localizacao='Valencia';
where Localizacao IN ('Valencia');

```

```sql
update table_name set column_name = value
where column_name in new_value

update filmes set assistido = true
where titulo in
('avatar');
```


````sql
SELECT * From registos_atletas
WHERE Id_equipa=130888
AND 
Posicao='Goalkeeper';
-- where Localizacao IN ('lisboa');
````




